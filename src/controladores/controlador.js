const usuario = require('./usuarios-controlador');
const categorias = require('./categorias-controlador');
const clientes = require('./clientes-controlador');
const produtos = require('./produtos-controlador');

module.exports = {
	usuario,
	categorias,
	clientes,
	produtos
};